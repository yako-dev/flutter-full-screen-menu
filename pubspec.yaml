name: full_screen_menu
description: Full screen menu for Flutter apps to give a user a broad variety of variants in beautiful way
version: 3.0.0
repository: https://github.com/yako-dev/flutter-full-screen-menu
issue_tracker: https://github.com/yako-dev/flutter-full-screen-menu/issues
topics:
  - menu
  - overlay
  - widget
  - ui

environment:
  sdk: ">=3.13.0 <4.0.0"
  flutter: ">=3.47.0"

dependencies:
  flutter:
    sdk: flutter
  material_ui: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
